public class MergSortInClass {

      private static void merge (int[] arr, int start, int mid, int end) {

            int[] temp = new int[end - start];

            int index = 0;
            int indexL = start;
            int indexR = mid;

            while (indexL < mid && indexR < mid) {

                  if
            }

      }
}
